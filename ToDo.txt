Problema 1:
	DashBoard Component -> al momento del login sarebbe carino mostrasse lo userName che però vuol dire fare una get lato server,
	 ha senso farne un'altra poi in un secondo momento per lo USerDetailComponent? perchè se faccio il redirect dallo userDetailComponent(dopo la login)
	al DashBoardComp si eliminano tutti i dati degli observable?
Soluzione 1: Uso Un observable creato al momento della login e che viene subscribato da dashboard e userDetail.
Soluzione 2: Dashboard inutile, viene visualizzata solo all'inizio

Microservizio 3: almeno due tabelle -> prodotti e carrello
	prodotti: id, nome, costo, img?, descrizione
	carrello: id prodotto, id user(superchiave)
    Endpoints:
		1)get prodotto
		2) get prodotti
		3) post prodotto
		4)put prodotto
		5) delete prodotto
		6) add carrello(prodotto, user)
		7) get prodotti carrello(user)
		8) delete prodotto d carrello(user)
		9) delete tutti i prodotti del carrello(user)
		10)
		 

